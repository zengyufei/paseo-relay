app = "replace-with-your-app"
primary_region = "replace-with-your-region"
kill_signal = "SIGTERM"
kill_timeout = "30s"

[build]
  dockerfile = "Dockerfile"

[env]
  PASEO_RELAY_HOST = "0.0.0.0"
  PASEO_RELAY_PORT = "4000"
  PASEO_RELAY_MIN_CLUSTER_SIZE = "2"

[http_service]
  internal_port = 4000
  force_https = true
  auto_stop_machines = "off"
  auto_start_machines = false

  [http_service.concurrency]
    type = "connections"
    soft_limit = 10000
    hard_limit = 15000

  [[http_service.checks]]
    interval = "10s"
    timeout = "2s"
    grace_period = "15s"
    method = "GET"
    path = "/ready"

[metrics]
  port = 4000
  path = "/metrics"

[deploy]
  strategy = "rolling"

[[vm]]
  memory = "2gb"
  cpu_kind = "performance"
  cpus = 1
